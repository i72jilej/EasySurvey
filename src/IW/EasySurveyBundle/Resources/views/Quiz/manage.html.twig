{% extends "IWEasySurveyBundle::layout.html.twig" %}
{% block content %}
      <div class="page-header">          
        <h1>{% trans %}Gestionar encuestas{% endtrans %}</h1>
      </div>  
      <div class="page-content">  
          <table>
              <tr>
                <th>Nombre</th>
                <th>Acciones</th>
              </tr>
              {% for data in quiz %}
              <tr>
                  <td> {{data.name}} </td>
                  <td> 
                      <a href="{{ path('iw_easy_survey_edit_quiz', {'id': data.id})}}"> Editar </a> |
                      <a href="{{ path('iw_easy_survey_delete_quiz', {'id': data.id})}}" onclick="return confirm('¿Esta seguro?')"> Borrar </a> 
                  </td>
              </tr>
              {% endfor %}
          </table>
      </div>
{% endblock %}